# GlyphEcho 0.6.0

本版整理主界面设置结构，并提高透明提示模式下重复次数的可读性。现有 `%LOCALAPPDATA%\GlyphEcho` 设置保持兼容，无需重新配置。

## 设置页

- 侧栏新增独立“设置”页面
- 开机自启、关闭窗口行为和 Windows 11 毛玻璃设置从概览迁移到专门页面
- 设置分为“启动与退出”和“窗口外观”，原有保存行为保持不变

## 提示可读性

- 重复次数 `× N` 增加独立底色、边框和圆角
- 低级透明提示模式下，计数不再直接叠在桌面背景上
- 深色与浅色色板均保持原有强调色

## 重要升级说明

0.3.1 / 0.4.0 的非系统盘便携用户仍需手动下载同通道的 0.6.0 ZIP，退出 GlyphEcho 后解压覆盖原目录；也可以直接运行 0.6.0 安装器。0.5.0 及更高版本可以正常执行后续跨卷自动更新。

## 下载

- `GlyphEcho-0.6.0-Full-Setup.exe` / `GlyphEcho-0.6.0-Lite-Setup.exe`
- `GlyphEcho-0.6.0-Full.zip` / `GlyphEcho-0.6.0-Lite.zip`

Lite 包需要 .NET 8 Desktop Runtime；Full 包已包含运行时。
