# GlyphEcho 0.5.1

本版集中修复配置、规则继承、输入监听和自动更新中的可靠性问题，并显著降低大按键目录和无手柄环境下的后台开销。现有 `%LOCALAPPDATA%\GlyphEcho` 设置会自动兼容迁移。

## 性能优化

- 全局按键目录改为共享索引，新增快捷键不再复制或遍历完整目录
- 应用规则只保存差异项，不再为每个应用缓存一份完整按键目录
- 前台应用信息和规则解析增加缓存，提示内容未变化时不再反复重建界面
- 自动记录按键使用 750ms 防抖合并保存，按键目录搜索使用 500ms 防抖
- 未连接手柄时，XInput 空端口轮询由每秒约 20 次退避到每秒 1 次

## 修复

- 修复合法配置中的空值可能导致启动失败，并在配置损坏时备份原文件和显示明确提示
- 设置改为落盘刷新后同目录替换，保存失败会反馈到界面
- 修复高级模式 Left/Right 修饰键与普通规则匹配不一致
- 修复应用规则单键设置、隐藏键继承和显式清空按键说明没有正确生效
- 修复手柄输入没有使用真实前台应用规则
- 修复更新检查可能被页面、托盘和后台同时启动；后台失败现在会写入诊断日志
- 修复更新器失败后不能可靠恢复启动旧程序，以及 PID 复用可能导致等待错误进程
- 修复提示内容不变时仍周期性重建 WPF 控件树

## 更新与发布可靠性

- 主程序与更新器统一使用同一套清单、RSA 签名、SHA-256 和下载地址验证
- 更新成功后即使旧备份清理失败，也不会删除已经安装的新版本
- CI 和本地正式打包会检查测试、publish、更新器、安装器及四个正式资产
- 图标像素未变化时，构建不再重写源 PNG

## 重要升级说明

0.3.1 / 0.4.0 的非系统盘便携用户仍需手动下载同通道的 0.5.1 ZIP，退出 GlyphEcho 后解压覆盖原目录；也可以直接运行 0.5.1 安装器。0.5.0 及更高版本可以正常执行后续跨卷自动更新。

## 下载

- `GlyphEcho-0.5.1-Full-Setup.exe` / `GlyphEcho-0.5.1-Lite-Setup.exe`
- `GlyphEcho-0.5.1-Full.zip` / `GlyphEcho-0.5.1-Lite.zip`

Lite 包需要 .NET 8 Desktop Runtime；Full 包已包含运行时。
