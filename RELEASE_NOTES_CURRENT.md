# GlyphEcho 0.5.0

本版新增深浅六种提示色板，修复相同低级提示重复分行和非系统盘便携版后续自动更新失败的问题。现有 `%LOCALAPPDATA%\GlyphEcho` 设置保持不变。

## 重要升级说明

0.3.1 / 0.4.0 的旧更新器无法在程序目录与系统临时目录位于不同磁盘时完成替换。受影响的便携用户需要进行一次手动升级：

1. 下载与当前通道对应的 0.5.0 ZIP。
2. 右键托盘图标并退出 GlyphEcho。
3. 将 ZIP 解压覆盖原程序目录。

也可以直接运行 0.5.0 安装器。用户设置位于 `%LOCALAPPDATA%\GlyphEcho`，覆盖程序文件或安装新版不会删除设置。升级到 0.5.0 后，后续跨卷自动更新可正常使用。

## 新功能

- 新增深色薄荷绿、天空蓝、琥珀黄三种提示色板
- 新增浅色深青、钴蓝、玫红三种提示色板
- 设置页用 `×1 / ×2 / ×3` 色块直接预览重复次数文字颜色，选择后立即保存

## 修复

- 低级提示现在按实际可见按键合并，修复相同按键因前台应用采样变化而重复分行的问题
- 中高级提示继续按可见来源和功能区分，不会误合并不同内容
- 更新器在目标目录同盘创建 stage 与 backup，修复便携目录和系统临时目录跨卷时替换失败

## 下载

- `GlyphEcho-0.5.0-Full-Setup.exe` / `GlyphEcho-0.5.0-Lite-Setup.exe`
- `GlyphEcho-0.5.0-Full.zip` / `GlyphEcho-0.5.0-Lite.zip`

Lite 包需要 .NET 8 Desktop Runtime；Full 包已包含运行时。
